# Database
POSTGRES_PASSWORD=change-me-in-production
DATABASE_URL=postgresql+asyncpg://dataprune:change-me-in-production@db:5432/dataprune
PROFILES_PATH=profiles.yaml

# Optional: API key for authentication (empty = auth disabled)
API_KEY=

# Optional: JSON array of allowed CORS origins (default: all)
CORS_ORIGINS=["*"]
